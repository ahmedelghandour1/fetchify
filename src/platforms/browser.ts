export { 
    DELETE, 
    GET, 
    HEAD, 
    PATCH, 
    POST, 
    PUT, 
    globalConfigs, 
    globalHeaders, 
    setInterceptors ,
    getParamsFromString,
    isBrowser,
    nop,
    replaceParamsInString,
    serializeObject,
    default
} from '@/main';

export type { 
    Configs, 
    FetchData, 
    FetchResult, 
    FetchedData, 
    Interceptors, 
    Method, 
    ResponseType, 
} from '@/main'
